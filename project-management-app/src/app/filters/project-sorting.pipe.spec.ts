import { ProjectSortingPipe } from './project-sorting.pipe';

describe('ProjectSortingPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectSortingPipe();
    expect(pipe).toBeTruthy();
  });
});
